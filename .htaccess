<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Transparently route all traffic to the /public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
